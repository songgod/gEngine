<UserControl x:Uid="UserControl_Project" x:Class="gEngine.Project.Controls.ProjectControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gEngine.Project.Controls"
             xmlns:cvt="clr-namespace:gEngine.Project.Converter"
             mc:Ignorable="d" 
             xmlns:prjcmds="clr-namespace:gEngine.Commands;assembly=gEngine.Commands"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <cvt:MapIndexConverter x:Uid="cvt:MapIndexConverter_1" x:Key="micvt"/>
    </UserControl.Resources>
    <Grid x:Uid="Grid_1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Uid="ColumnDefinition_1" Width="5*"/>
            <ColumnDefinition x:Uid="ColumnDefinition_2" Width="1*"/>
        </Grid.ColumnDefinitions>
        <local:MapsControl x:Uid="PART_MapsControl" Grid.Column="0" x:Name="PART_MapsControl" MapsSource="{Binding Path=Project.OpenMaps, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=local:ProjectControl}}"/>
        <local:LayerMgrControl x:Uid="PART_LayerMgrControl" Grid.Column="1" x:Name="PART_LayerMgrControl"
                               MapSource="{Binding Path=Project.OpenMaps.CurrentMap, RelativeSource={RelativeSource AncestorType=local:ProjectControl},Mode=OneWay}"/>
    </Grid>
</UserControl>
